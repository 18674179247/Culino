请始终使用中文回复。

## 规则

- UI 组件统一使用 Material 3 (`androidx.compose.material3`)，禁止引入 Material 2 (`androidx.compose.material`) 组件依赖
- Material Icons (`androidx.compose.material.icons`) 可以正常使用，它与 Material 2 组件库无关
- 包名统一使用 `com.culino`
